name: crud_case_study
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  build_runner: ^2.4.9
  dio: ^5.4.3+1
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.9.3
  json_serializable: ^6.8.0
  mobx: ^2.3.3+2
  mobx_codegen: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
